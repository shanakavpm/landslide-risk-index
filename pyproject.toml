[tool.ruff]
target-version = "py312"
line-length = 100
exclude = ["data", "outputs", ".venv"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

